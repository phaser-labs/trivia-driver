/**
 * ModalQuestion - DEPRECADO
 * 
 * Este componente ha sido migrado a Phaser como DOMElements.
 * El modal ahora se renderiza directamente desde MainScene.ts usando:
 * - showQuestionModal(): Crea y muestra el modal
 * - closeQuestionModal(): Cierra y destruye el modal
 * - handleModalAnswer(): Procesa la respuesta seleccionada
 * 
 * El modal sigue usando HTML/CSS pero es manejado completamente por Phaser.
 * Ver: src/games/game-car-quiz/game/scenes/MainScene.ts
 */